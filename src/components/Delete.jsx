import { useState } from 'react';

function Delete() {

  const [inputIndex, deleteInput] = useState[index];

  const buttonClicked(index) => {
    inputContainer.splice[index];
    console.log[inputContainer]

}

export default Delete;